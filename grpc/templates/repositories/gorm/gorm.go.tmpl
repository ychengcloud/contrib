package repogorm

import (
	"gorm.io/gorm"

	"{{ .Package }}/repositories"
)

const (
	MaxLimit = 1000
)

type repository struct {
	repositories.Repository
	db *gorm.DB
}

func New{{ .Project | pascal }}Repository(db *gorm.DB) *repository {
	return &repository{
		db: db,
	}
}
